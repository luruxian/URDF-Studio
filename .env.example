# Cross-origin mesh preview (Agile Robot attachment URLs). localhost ≠ 127.0.0.1 —
# list both when robots backend is opened either way.
VITE_MESH_PREVIEW_ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# Agile Robot integration: origin(s) allowed to send robots:studio-bootstrap postMessage
# (hash bootstrap is primary; postMessage is optional fallback).
# Comma-separated, supports * wildcard (e.g. https://*.enkeebot.com,https://*.enkeebot.cn).
# Local main-site: http://localhost:5173,http://127.0.0.1:5173
VITE_AGILE_ROBOT_ORIGINS=https://*.enkeebot.com,https://*.enkeebot.cn

# AI requires robots bootstrap (VITE_ROBOTS_API_BASE_URL) or managed backend (VITE_AI_BACKEND_URL).
# No BYOK API keys are read from the browser env surface.

# robots managed AI (production)
# VITE_ROBOTS_API_BASE_URL=https://api.enkeebot.com/api/v1

# URDF+STL mesh regenerate: client poll while waiting for Team Mesh job (ms)
# VITE_MESH_JOB_POLL_INTERVAL_MS=5000
# VITE_MESH_JOB_POLL_TIMEOUT_MS=900000

# Local-only: allow opening Studio without robots handoff
# VITE_ALLOW_STANDALONE=1
